// store the nams of friends in an array
const names = String[''] = ["Abrar", "Fariha", "Laiba", "Bilal"]

// print each person's name from the array
for (const name of names){
    console.log(name);
    }